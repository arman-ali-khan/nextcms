import UserLayout from '@/Layout/User/UserLayout';

const index = () => {
    return (
        <UserLayout title={'Author Profile'}>
            
        </UserLayout>
    );
};

export default index;